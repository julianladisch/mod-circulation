name: Maven central workflow

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  maven:
    runs-on: ubuntu-latest
    steps:
      - run: mvn test | tee out
      - run: tail -1000 out
